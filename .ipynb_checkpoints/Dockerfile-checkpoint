FROM python:3.10-slim

WORKDIR ./
COPY . .
RUN pip install --no-cache-dir gradio
RUN pip install torch, pandas, numpy, matplotlib, scikit-learn
EXPOSE 7860
ENV GRADIO_SERVER_NAME="0.0.0.0"

CMD ["python", "app.py"]